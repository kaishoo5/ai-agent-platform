function LoginPage() {
    return (
        <main className="login-page">
            <div className="login-card">
                <h1>AI Agent</h1>
                <p>로그인 기능은 이후 JWT 단계에서 구현합니다.</p>
            </div>
        </main>
    );
}

export default LoginPage;